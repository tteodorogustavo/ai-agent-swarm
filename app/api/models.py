# models.py
"""
This file defines the data models using Pydantic.
It specifies the exact structure of the data that the API accepts (requests) and returns (responses).
Purpose: Input validation and security.
It ensures that only well-formed and valid data reaches the core logic, rejecting malformed requests automatically.
"""